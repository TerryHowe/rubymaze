<%- @sep = "" %>
{
    "data": [<%- @passages.each do |passage| %><%= @sep %>{
        "id": <%= passage.id %>,
        "source_id": <%= passage.source.id %>,
        "destination_id": <%= passage.destination.id %>,
        "direction": "<%= passage.direction %>"
    }<% @sep = ", " %><% end %>]
}
